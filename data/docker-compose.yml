version: '3.7'

services:
  jupyterlab:
    image: jupyter/tensorflow-notebook:latest
    environment:
      ENV JUPYTER_ENABLE_LAB=yes
      ENV GEN_CERT:yes
    ports:
      - 7777:7777
    volumes:
      - ./:/home/jovyan/data